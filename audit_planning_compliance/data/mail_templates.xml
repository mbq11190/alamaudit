<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data noupdate="1">
        <record id="email_template_pbc_reminder" model="mail.template">
            <field name="name">PBC Reminder</field>
            <field name="model_id" ref="model_audit_pbc_request"/>
            <field name="email_from">${object.engagement_id.engagement_manager_id.email or user.email or ''}</field>
            <field name="email_to">${object.responsible_client_contact.email or object.engagement_id.client_partner_id.email or ''}</field>
            <field name="subject">Reminder: ${object.name}</field>
            <field name="body_html"><![CDATA[
                <p>Dear ${object.responsible_client_contact.name or 'Client'},</p>
                <p>This is a reminder to provide <strong>${object.name}</strong> for engagement <strong>${object.engagement_id.name}</strong>.</p>
                <p>The current due date is ${object.due_date}. Kindly upload the evidence via the portal or email the signed documents.</p>
                <p>Regards,<br/>${object.engagement_id.engagement_manager_id.name or object.engagement_id.engagement_partner_id.name}</p>
            ]]></field>
        </record>
    </data>
</odoo>

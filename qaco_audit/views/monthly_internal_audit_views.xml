<odoo>
    <data>
        <!-- Monthly tree (commented out; model qaco.monthly.internal.audit is missing) -->
        <!--
        <record id="view_monthly_internal_audit_tree" model="ir.ui.view">
            <field name="name">qaco.monthly.internal.audit.tree</field>
            <field name="model">qaco.monthly.internal.audit</field>
            <field name="arch" type="xml">
                <tree>
                    <field name="seq_code"/>
                    <field name="client_id"/>
                    <field name="month_id" string="Year-Month"/>
                    <field name="stage_id"/>
                </tree>
            </field>
        </record>
        -->

        <!-- Removed view_monthly_internal_audit_form (form record omitted because model qaco.monthly.internal.audit is not present) -->
    </data>
</odoo>
